{
  "name": "liho",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "setup": "npm --prefix packages/contracts install && npm --prefix api install && npm --prefix web install",
    "dev": "concurrently -n api,web -c green,cyan \"npm --prefix api run dev\" \"npm --prefix web run dev\"",
    "dev:api": "npm --prefix api run dev",
    "dev:web": "npm --prefix web run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
